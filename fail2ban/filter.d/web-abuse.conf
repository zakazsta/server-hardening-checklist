# /etc/fail2ban/filter.d/web-abuse.conf
# Bans clients generating bursts of 401/403/404/429 (typical of brute-force / scanners).
[Definition]
failregex = ^.*"(?:GET|POST|HEAD).*" (?:401|403|404|429) .*"<HOST>".*$
ignoreregex =
